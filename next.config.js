/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  }
  // import {imgUrls} from './pages/posts/[id]'🤠
  
  module.exports = {
    nextConfig,
    images: {
        domains: ["db3pap006files.storage.live.com"],
      }
  }